## 🗂️ Иерархия сущностей в Trello

### 👤 Участники (Members)
- Пользователи системы.  
- Могут быть участниками разных досок.  
- Уровень доступа зависит от роли: **админ**, **обычный участник**, **наблюдатель**.  

---

### 📋 Доски (Boards)
- Главная рабочая область.  
- На доске находятся списки и карточки.  
- У доски есть участники, которые могут её видеть и редактировать.  

---

### 🗃️ Списки (Lists)
- Колонки внутри доски (например: *"To Do"*, *"In Progress"*, *"Done"*).  
- Списки принадлежат конкретной доске.  
- Внутри списка находятся карточки.  

---

### 📝 Карточки (Cards)
- Конкретные задачи или элементы работы.  
- Принадлежат списку, который находится на доске.  
- У карточки могут быть:
  - назначенные участники,  
  - метки,  
  - комментарии,  
  - вложения.
~~~
Member (создатель)
   └── Board (доска)
          ├── Members (участники с ролями)
          └── Lists → Cards
~~~

🔗 Иерархия обязательных связей
~~~
Board (доска)
   └── List (список)
           └── Card (карточка)
~~~
- Board — корневая сущность.
- List не может существовать без доски.
- Card не может существовать без списка (а значит и без доски).
- Но List может быть пустым, без карточек.


# 📚 Справочник Trello REST API

## 👤 Members (участники)
| Операция | Endpoint |
|----------|----------|
| Получить информацию о пользователе | `GET /members/{id}` |
| Доски участника | `GET /members/{id}/boards` |
| Карточки участника | `GET /members/{id}/cards` |
| Управление правами доступа | через доски/карточки |

---

## 📋 Boards (доски)
| Операция | Endpoint |
|----------|----------|
| Создать доску | `POST /boards` |
| Получить доску | `GET /boards/{id}` |
| Обновить доску | `PUT /boards/{id}` |
| Удалить доску | `DELETE /boards/{id}` |
| Получить списки | `GET /boards/{id}/lists` |
| Получить карточки | `GET /boards/{id}/cards` |
| Получить участников | `GET /boards/{id}/members` |

---

## 🗃️ Lists (списки)
| Операция | Endpoint |
|----------|----------|
| Создать список | `POST /lists` |
| Получить список | `GET /lists/{id}` |
| Обновить список | `PUT /lists/{id}` |
| Получить карточки | `GET /lists/{id}/cards` |
| Архивировать список | `PUT /lists/{id}/closed` |

---

## 📝 Cards (карточки)
| Операция | Endpoint |
|----------|----------|
| Создать карточку | `POST /cards` |
| Получить карточку | `GET /cards/{id}` |
| Обновить карточку | `PUT /cards/{id}` |
| Удалить карточку | `DELETE /cards/{id}` |
| Переместить карточку | `PUT /cards/{id}?idList={listId}` |
| Добавить участника | `POST /cards/{id}/idMembers` |
| Добавить комментарий | `POST /cards/{id}/actions/comments` |
| Прикрепить файл | `POST /cards/{id}/attachments` |

---

## 📌 Actions (действия)
| Операция | Endpoint |
|----------|----------|
| Получить действие | `GET /actions/{id}` |
| Обновить действие | `PUT /actions/{id}` |
| Удалить действие | `DELETE /actions/{id}` |
| Получить поле | `GET /actions/{id}/{field}` |
| Доска/карточка/список/участник | `GET /actions/{id}/board` и др. |
| Обновить текст комментария | `PUT /actions/{id}/text` |
| Реакции | `GET/POST/DELETE /actions/{idAction}/reactions` |
| Сводка реакций | `GET /actions/{idAction}/reactionsSummary` |

---

## 🏢 Organizations (рабочие пространства)
| Операция | Endpoint |
|----------|----------|
| Создать организацию | `POST /organizations` |
| Получить организацию | `GET /organizations/{id}` |
| Обновить организацию | `PUT /organizations/{id}` |
| Удалить организацию | `DELETE /organizations/{id}` |

---

## 🔔 Webhooks
| Операция | Endpoint |
|----------|----------|
| Создать webhook | `POST /webhooks` |
| Получить список | `GET /tokens/{token}/webhooks` |
| Удалить webhook | `DELETE /webhooks/{id}` |

---

## 🔑 Tokens
| Операция | Endpoint |
|----------|----------|
| Получить информацию о токене | `GET /tokens/{token}` |
| Отозвать токен | `DELETE /tokens/{token}` |
